# RT-Smart BitBake 基础配置
# 基础构建配置
BUILD_ARCH = "${@os.uname()[4]}"
BUILD_OS = "${@os.uname()[0].lower()}"
BUILD_SYS = "${BUILD_ARCH}-${BUILD_OS}"

# 工作目录配置
TMPDIR ?= "${TOPDIR}/tmp"
WORKDIR = "${TMPDIR}/work/${MULTIMACH_TARGET_SYS}/${PN}/${PV}-${PR}"
S = "${WORKDIR}/${BP}"
B = "${S}"

# 基础变量定义
BP = "${BPN}-${PV}"
BPN = "${@bb.parse.vars_from_file(d.getVar('FILE', False),d)[0] or 'defaultpkgname'}"
PV = "${@bb.parse.vars_from_file(d.getVar('FILE', False),d)[1] or '1.0'}"
PR = "r0"
PF = "${PN}-${PV}-${PR}"
PN = "${@bb.parse.vars_from_file(d.getVar('FILE', False),d)[0] or 'defaultpkgname'}"

# 目标系统设置
TARGET_ARCH ?= "${MACHINE_ARCH}"
TARGET_OS = "linux"
TARGET_SYS = "${TARGET_ARCH}-${TARGET_OS}"
MULTIMACH_TARGET_SYS = "${MACHINE_ARCH}-${TARGET_OS}"

# 机器架构设置
MACHINE_ARCH = "${MACHINE}"

# 下载目录
DL_DIR ?= "${TOPDIR}/downloads"

# 并行设置
BB_NUMBER_THREADS ?= "4"
PARALLEL_MAKE ?= "-j 4"

# 基础任务
BB_DEFAULT_TASK ?= "build"

# 包含机器和发行版配置
include conf/machine/${MACHINE}.conf
include conf/distro/${DISTRO}.conf 